# typechecker


```python
@checktypes(types=[type1,type2,...])
def example(a,b,...):
  ...
  
these consts can be used in place of types
not_none: the parameter can be antyhing but none
anytype: the parameter can be any type
```
